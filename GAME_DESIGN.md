# 삼국지 카드 배틀 - 기획 문서

## 1. 개요
- 장르: 확률형 카드 수집 + 배틀
- 소재: 삼국지 장수 (초기 100명 이상 규모)
- 그래픽: 웹 기반, 귀여운 도트(픽셀) 스타일
- 플레이 방식: 방 코드 생성/초대 기반 멀티플레이. 비로그인(닉네임만 입력)

## 2. 카드(장수) 시스템
- 등급: S / A / B / C / D / E (6단계, S가 최상위)
- 장수 한 명당 S~E 6개 등급 카드가 전부 존재 (예: 여포 S/A/B/C/D/E, 손상향 S/A/B/C/D/E).
  같은 장수라도 등급에 따라 스탯·스킬 위력이 달라짐 — 뽑기에서는 등급을 먼저 정한 뒤
  그 등급의 장수 카드 중 하나가 랜덤으로 나옴
- 강화 시스템 존재 (등급 내에서 카드 성장)
- 장비 시스템 없음 (장수 카드 자체로만 구성)
- 장수 로스터: 위/촉/오/군웅에서 널리 알려진 장수 125명 x 6등급 = 750장 카드 시드 완료
  (`backend/app/seed_data.py`)
  - 여성 장수 20명 포함 (초선/손상향/대교/소교/황월영/축융부인 등) — 최초 로스터에 누락되어 있던 것을 발견해 보강
  - 유비/손권도 누락되어 있어 조조와 함께 삼국 군주 3인이 모두 들어가도록 보강
  - 하후돈은 팽성 전투에서 한쪽 눈을 잃은 설정을 스킬 설명과 일러스트에 반영
- 카드 일러스트: AWS Bedrock 기반 생성 (`backend/dev_scripts/illustration_agent.py`)
  1. 텍스트 모델 5개(Claude Haiku 4.5, Nova Pro/Lite/2 Lite 계열)를 쓰로틀링 시 순서대로
     돌려가며 장수 설명 → 영어 이미지 프롬프트로 변환
  2. Nova Canvas(`amazon.nova-canvas-v1:0`)로 실제 이미지 생성 후 다운샘플+색상 제한+
     최근접보간 업샘플로 후처리해 도트 느낌을 냄
  3. Nova Canvas가 이 계정에서 Legacy로 표시되어 있어 간헐적으로 실패하는데, 재시도로 대부분
     해결됨 (125명 중 124명 AI 생성 성공, 1명만 절차적 렌더러로 대체)
  4. 완전히 실패하면 `generate_portraits.py`의 절차적 렌더러(세력 색상+유형별 무기 실루엣
     조합)로 자동 대체 — 항상 뭔가는 생성되도록 함
  - `--kind scene` 옵션으로 장수 외 다른 이미지(배경/아이템 등)도 생성 가능하도록 범용화
  - 결과: `frontend/assets/portraits/<장수이름>.png`, 생성 방식은 `_manifest.json`에 기록

### 스탯 구조
- 기본 스탯: 지력, 무력, 통솔력, 매력, 정치력
- 전투용 스탯: HP, MP, 공격력
- 기본 스탯 중 실제로 전투에 쓰이는 건 무력(선공 순서)과 통솔력(방어력 대용) 2개뿐이고,
  지력/매력/정치력은 현재 전투 로직에는 관여하지 않는 표시용 스탯
- 모든 스탯은 "C등급 기준치 x 등급 가중치(`RARITY_WEIGHT`, E0.65~S2.00) x 유형별 배율
  x 이름 기반 편차(비율)"로 계산됨 (`backend/app/seed_data.py`) — 등급 가중치 숫자 하나만
  바꾸면 그 등급의 모든 전투 스탯이 함께 오르내림
- HP와 공격력도 유형(무력형/통솔형/정치형 등) 배율을 타므로 같은 등급 안에서도
  장수마다 체력 약 2배, 공격력 약 2.5배까지 차이가 남
- 피해 = 공격력 − 방어력(통솔력) x 0.35. 예전에는 공격력 기준치가 방어 기여분보다
  낮아 피해가 최소값 1로 깎이고 전투가 라운드 제한까지 가는 일이 잦았음
- 턴 제한은 12라운드. 피해 난수(±30%)를 넣은 뒤 다시 잰 값은 중앙 9 / 평균 9.1라운드,
  90%가 12라운드 안에 끝난다 (C덱 200판). 정상 전투는 그대로 두고, 늘어지는 13%만
  일기토로 넘어간다
- 승부는 전멸승(rout)과 일기토승(deathmatch)으로 나뉜다. 12라운드 동안 승부가 나지
  않으면 각 진영에서 무작위로 한 명씩 뽑아 별도 화면에서 1:1 일기토로 결판을 낸다
  (`run_deathmatch`). 체력/MP를 채우고 시작하며, 합을 주고받을수록 피해가 세져서
  반드시 끝난다
- 장수별 고유 스킬 1개 보유 (스킬 위력도 등급별로 별도 고정값 `SKILL_POTENCY`)

## 3. 뽑기(가챠) 시스템
- 링(뽑기 재화) 획득: 무과금 경로 기본
- 링 구매: 실제 결제 없이 즉시 지급되는 임시(mock) 기능 (`POST /players/{id}/purchase_rings`,
  `backend/app/main.py`의 `RING_PACKAGES`). 나중에 실제 PG 연동으로 교체할 자리이며,
  상품 구조(package_id → 링 수량/가격표시)는 그대로 재사용 가능
- 프론트: 뽑기 화면과 카드 보관함/덱 편성을 한 화면(`screen-cards`)으로 통합 —
  뽑자마자 바로 아래 카드 그리드에 반영되어 덱을 짤 수 있음. 상단바의 "링 구매" 버튼으로
  언제든 구매 모달을 열 수 있음. "덱 초기화" 버튼으로 선택을 한 번에 비울 수 있음
- 카드 등급 표현: C 이상부터 카드 위로 빛이 훑고 지나가는 반짝임 효과, A/S는
  테두리가 지속적으로 은은하게 펄스하는 발광 애니메이션 (등급이 높을수록 더 화려하고 빠름)
- MVP 1순위 개발 대상 (완료)

## 4. 배틀 시스템
- 방식: 턴제 자동전투
- 덱 구성: 장수 5장
- 방어력: 별도 스탯 없이 통솔력(leadership)을 방어력으로 사용
- 강화 시스템: 강화 레벨 1당 hp/공격력/방어력(통솔력) +5%

### PvP (1:1, 순차 대결)
- 개인 덱(5장) vs 개인 덱(5장)
- 1번 장수끼리 붙어 한쪽이 쓰러지면 다음 장수로 순서대로 진행
- 선공은 두 장수 중 무력이 높은 쪽
- 이긴 장수는 남은 HP를 그대로 이어서 다음 상대와 싸움 (누적)
- 스킬 발동: 기본 공격마다 MP가 차오르고, MP가 가득 차면 그 턴에 고유 스킬이 자동 발동 (게이지형)
- 고유 스킬은 damage(피해) / heal(회복) / buff(아군 강화) / debuff(적 약화) 4가지 효과로 구현
  (세부 효과 매핑은 `backend/app/seed_data.py`, 전투 계산은 `backend/app/battle.py` 참고)
- 전투 결과는 문자열 로그가 아니라 구조화된 이벤트 목록(`events`)으로 반환 — 각 이벤트가
  행동 주체/대상/수치/결과 HP를 담고 있어 프론트에서 한 번에 하나씩 재생 가능
- 프론트: 배틀 화면에서 "다음" 버튼으로 이벤트를 한 스텝씩 확인하거나 "전체 보기"로 스킵
  가능. 두 장수 초상화 + 고유 스킬 이름 + HP바 + MP바(게이지) + ATK를 배틀필드 배경 이미지
  위에 크게 보여주고, 공격 시 공격자가 상대 쪽으로 달려드는 연출(lunge) + 피격 스파크 +
  피격/회복/버프/디버프/빗나감마다 다른 색 플래시 애니메이션으로 액션 연출
  (`frontend/app.js`의 `applyBattleEvent`).
  진행 버튼(다음/전체보기/로비로)은 로그 위쪽에 고정되어 로그가 길어져도 밀려나지 않음
- 스킬 발동은 기본 공격과 구분되게 연출: 황금색 플래시/대형 스파크 + 화면 상단에
  "⚡ OOO의 「스킬명」 발동!" 배너가 잠깐 떴다 사라짐 — MP 게이지가 실제로 어떻게
  차서 스킬로 이어지는지 눈으로 볼 수 있음
- 배틀필드 배경(`frontend/assets/scenes/battlefield.png`)도 Bedrock 일러스트 에이전트로 생성
- 구현/테스트 완료: `POST /battle/simulate` 로 5:5 덱 배틀 시뮬레이션 가능

### 턴 진행과 플레이어 선택
- 두 진영은 예외 없이 한 번씩 번갈아 행동한다. 한 라운드 안에서 각 진영은 자기 카드를
  한 번씩만 쓸 수 있고, 한쪽이 쓸 카드를 다 쓰면 양쪽 모두 다음 라운드로 넘어간다.
- 한 차례에 플레이어가 고르는 것은 최대 세 가지다.
  1. **행동할 장수** — 아직 이번 라운드에 움직이지 않은 내 카드 중 하나 (`await_actor`)
  2. **스킬 사용 여부** — MP가 가득 찼을 때만 물어본다. 아끼기로 하면 MP는 그대로 남아
     다음 차례에 다시 쓸 수 있다 (`await_action`)
  3. **대상** — 스킬/공격이 향할 상대 (`await_target`)
- 고를 수 있는 카드는 그동안만 스킬 효과를 펼쳐 보여준다 (타입 · 효과 문구 · MP 상태).
  평소에 다 펼쳐두면 화면이 빽빽해지고, 고를 때는 툴팁을 하나씩 올려봐야 후보를
  비교할 수 있었기 때문에 이 순간에만 꺼낸다 (`cardSlotHtml`의 `battle.pickKeys`).
- 모든 선택은 20초 안에 답하지 않으면 AI가 대신 고른다 (장수=무력 최고, 스킬=사용,
  대상=HP가 가장 낮은 적).
- 위임 토글: 로비 체크박스와 전투 화면의 "이후 AI에게 맡기기" 버튼은 같은 상태를 바꾼다.
  전투 도중에 켜면 지금 기다리고 있던 질문까지 즉시 AI에게 넘어가므로, 이번 차례부터
  바로 적용된다. 다시 끄면 그 다음 질문부터 직접 고른다.
- 서버는 이벤트를 흘려보내면서 곧바로 질문을 던지므로 연출이 밀릴 수 있다. 선택창이 뜨는
  순간 프론트는 그 시점까지 밀린 이벤트만 텀 없이 몰아 재생해 판을 맞춘다
  (`catchUpBattleQueue`).

### 전투의 무작위성과 진영 효과 (2026-09 조정)
- **피해량 난수 ±30%** (`DAMAGE_VARIANCE`). 이전에는 `_calc_damage`가 완전히
  결정론적이어서(유일한 난수는 5% 빗맞음) 같은 덱끼리 붙으면 늘 같은 쪽이 이겼다.
  재도전이 무의미했고, 시나리오 난이도가 0% 아니면 100%로만 나왔다.
- **진영 전체 버프/디버프는 3라운드만 지속** (`TEAM_EFFECT_ROUNDS`). 이전에는 영구히
  곱해져서, '적 전체 공격력 -46%' 한 방이면 상대 공격력이 전투 끝까지 절반으로 묶였다.
  그 스킬을 가진 쪽이 사실상 이기는 구조였다(실측 승률 100% vs 0%).
  `SideState`가 남은 라운드를 들고 있고, 풀리면 `side_effect_end` 이벤트가 나간다.
  `rounds=None`으로 걸면 안 풀린다 - 시나리오 스테이지 보정이 이걸 쓴다.
- 선공은 무력이 가장 높은 카드를 가진 쪽이 가져가며, 선공 측 승률은 약 62%다.

## 5. 시나리오 (싱글 캠페인)
- 역사적 전투 12개 x Lv1~Lv5 = 60 스테이지. 전투 안에서만 순차 해금(Lv1을 깨야 Lv2),
  12개 전투 자체는 처음부터 모두 열려 있다.
- 덱: 내 보유 덱이 기본. 원하면 그 전투의 고정덱(역사적 주역 5명)을 고를 수 있고,
  카드가 없어도 무난히 깰 수 있는 안전한 선택지이므로 **보상이 60%로 줄어든다.**
- 적 덱은 손으로 60개를 적지 않고 **전투별 장수 풀 + 레벨별 등급 사다리**로 조합한다
  (`build_enemy_lineup`). 풀 앞 3명(주장 + 부장 2)은 모든 레벨에 나오고 나머지 2자리만
  돌아간다. 5자리를 다 돌리면 Lv5 관도대전에 안량/문추가 빠지고 문관만 남는 식으로
  최고 난이도가 오히려 물러졌다.
- **난이도는 캘리브레이션으로 맞춘다.** 등급을 똑같이 맞춰도 장수의 스탯·스킬 궁합
  때문에 고정덱 승률이 전투마다 0%~100%로 튄다. 그래서 `STAGE_ENEMY_POWER`는 손으로 쓴
  값이 아니라 실제 엔진을 돌려 목표 승률(Lv1 85% -> Lv5 55%, 고정덱 + AI 위임 기준)에
  맞춘 결과다. 장수 스탯이나 전투 공식을 건드리면 다시 뽑아야 한다:
  `python dev_scripts/calibrate_scenario.py`
- 보상: 클리어 시 `40 + Lv x 20`링(고정덱은 60%), 최초 클리어는 2배. 패배해도 10링.
  최초 클리어는 (전투, 레벨, 덱 모드)별로 따로 기록해서, 고정덱으로 먼저 깨도 나중에
  내 덱으로 다시 깨면 보너스를 또 받는다.

| Lv | 적 등급 | 고정덱 등급 | 목표 승률 | 내 덱 클리어 / 최초 | 고정덱 클리어 / 최초 |
|----|---------|------------|----------|--------------------|---------------------|
| 1  | D D D E E | D | 85% | 60 / 120 | 36 / 72 |
| 2  | C C C D D | C | 78% | 80 / 160 | 48 / 96 |
| 3  | B B B C C | B | 70% | 100 / 200 | 60 / 120 |
| 4  | A A A B B | A | 62% | 120 / 240 | 72 / 144 |
| 5  | S S S A A | S | 55% | 140 / 280 | 84 / 168 |

### 12개 전투

| 전투 | 연도 | 적 주장 | 고정덱 |
|------|------|--------|--------|
| 황건적의 난 | 184 | 장각 | 유비·관우·장비·공손찬·조조 |
| 반동탁 연합 | 190 | 동탁 | 원소·조조·유비·관우·장비 |
| 관도대전 | 200 | 원소 | 조조·하후돈·허저·장료·서황 |
| 장판파 | 208 | 조조 | 조운·장비·유비·제갈량·감부인 |
| 적벽대전 | 208 | 조조 | 주유·제갈량·황개·감녕·정보 |
| 화용도 | 208 | 조조 | 관우·관평·요화·장비·조운 |
| 합비전투 | 215 | 장료 | 손권·감녕·능통·태사자·여몽 |
| 한중공방전 | 219 | 조조 | 유비·법정·황충·조운·마초 |
| 정군산 | 219 | 하후연 | 황충·법정·엄안·유비·위연 |
| 번성전투 | 219 | 조인 | 관우·관평·요화·마량·유비 |
| 이릉대전 | 222 | 육손 | 유비·황충·마량·조운·요화 |
| 오장원 | 234 | 사마의 | 제갈량·강유·위연·왕평·마대 |

- 배경은 전투마다 전용 이미지 1장(`frontend/assets/scenes/battle_<key>.png`).
  황건적의 난과 반동탁 연합 배경은 첫 프롬프트가 Nova Canvas 콘텐츠 필터에 전부
  막혀서("burning farmland", "village ablaze", 치켜든 무기 등), 인물·폭력 묘사를 빼고
  풍경과 깃발 위주로 다시 써서 통과시켰다.
- 125명 중 시나리오에 등장하는 장수는 78명. 나머지 47명(여성 장수 다수, 후기 인물 등)은
  가챠·대전 전용이다.

### 구현

- `app/scenario_data.py` - 전투 12개, 등급 사다리, 캘리브레이션 표, 보상 계산
- `app/scenario.py` - 진행도 조회/기록, 해금 판정, 정산, 시나리오 덱 로딩
- `scenario_progress` 테이블 - 클리어한 스테이지만 한 줄씩. UNIQUE(player_id,
  battle_key, level, deck_mode)로 덱 모드별 최초 클리어를 구분한다
- API
  - `GET /scenario/battles?player_id=` 전투 12개 + 레벨별 해금/클리어/보상
  - `GET /scenario/battles/{key}/levels/{lv}?player_id=` 적 진용·고정덱 미리보기
  - `POST /rooms/{code}/scenario` 방을 스테이지로 전환
- **전투는 기존 방+AI 경로를 그대로 재사용한다.** 전투 화면, 장수/스킬/대상 선택창,
  위임 토글, 일기토가 전부 따라오므로 시나리오용 전투 코드는 따로 없다.
  - `RoomPlayer.lineup` - 보유 카드(player_cards)를 거치지 않는 덱 명단.
    적 덱과 고정덱이 이걸 쓴다
  - `Room.scenario` - 정산과 적 진영 보정에 필요한 스테이지 정보.
    시나리오 방은 사람이 항상 A, 적이 B로 고정된다(보정을 걸 진영이 정해져야 하므로)
- 화면: 입장 화면 `[시나리오]` 탭(기본) -> 전투 목록 12타일(배경 + 연도 + 별 n/5)
  -> 레벨 Lv1~5(잠김/클리어) -> 적 진용·고정덱 미리보기 + 보상 비교 -> 도전
  - 고정덱은 덱 편성을 건너뛰고 바로 전투. 내 덱은 5장을 이미 골라뒀으면 바로 시작하고,
    아니면 편성 화면으로 보낸 뒤 제출 시점에 시작한다
  - **원정 중에는 웹소켓을 끊지 않는다.** 끊기면 서버가 방에서 플레이어를 빼서 다음
    스테이지 진입이 막힌다 (`leaveToHome`이 로비 대신 전투 목록으로 보내는 이유)

### PvE (협동)
- 여러 플레이어가 각자 덱(5장)을 내세워 하나의 거대 보스를 동시에 공격
- (진행 방식 세부 로직은 PvP 엔진 확장 예정, 아직 미구현)

## 6. 멀티플레이 / 네트워크
- 실시간 동기화 범위: 배틀 + 로비 + 협력 플레이 전체
- 매칭: 방 코드를 생성하고 친구를 초대해 입장
- 계정: 로그인 없음, 닉네임만 입력하여 접속
- 방 최대 인원: 4명 (지금은 PvP 1:1만 지원하므로 2명이 덱을 제출해야 전투 시작,
  3~4명 참여는 추후 PvE 협동전 엔진이 준비되면 활용)
- 배포 단계:
  1. 같은 공유기(같은 네트워크) 내에서 우선 동작
  2. 이후 클라우드 등 원격 서버 배포로 자연스럽게 확장 가능한 구조로 설계

### 구현 (백엔드, `backend/app/rooms.py` + `main.py`)
- 방 상태(플레이어 목록/웹소켓 연결)는 서버 메모리에만 보관, 플레이어/카드는 SQLite에 영속
- 방 생성 시 제목을 함께 지정 (`title`), 방 코드와 별개로 목록에서 사람이 알아볼 수 있게 함
- `GET /rooms` → 입장 가능한(꽉 차지 않은) 방 목록: 제목 + (현재인원/최대인원)
- `POST /rooms` {nickname, title} → 방 생성 (방장), room_code 발급
- `POST /rooms/{room_code}/join` {nickname} → 방 코드로 입장
- `WS /ws/rooms/{room_code}?player_id=...` → 실시간 연결
  - 서버→클라이언트: `lobby_update`(제목/인원/준비 상태), `battle_result`(전투 이벤트+승자), `error`
  - 클라이언트→서버: `{"type":"ready"}`, `{"type":"submit_deck","deck":[player_card_id x5]}`
  - 정확히 2명이 덱을 제출하면 자동으로 `battle.py` 시뮬레이션이 돌고 결과가 방 전체에 브로드캐스트됨
- 프론트 입장 화면: 방 코드 직접 입력 또는 방 목록에서 클릭으로 바로 참가 가능
- 동작 확인: `backend/dev_scripts/smoke_test_rooms.py`, `pw_drive_battle.py`로 방 생성→입장→
  로비 동기화→가챠→덱 제출→배틀 결과(단계별 재생 포함) 전체 플로우 테스트 완료

## 7. 기술 스택 (초기)
- 백엔드: Python (FastAPI + WebSocket 계열)
- 프론트엔드: 바닐라 HTML/CSS/JS (이 PC에 Node.js가 없어서 React 대신 선택,
  빌드 단계 없이 FastAPI가 `frontend/`를 정적 파일로 서빙)
- DB: SQLite (초기 단순 구성, 추후 확장 가능하도록 설계)
- 도트 느낌은 "Press Start 2P" 픽셀 폰트 + 등급별 색상 테두리로 표현
  (한글은 가독성을 위해 시스템 폰트로 폴백)

## 8. 개발 우선순위 (MVP)
1. ~~링(가챠) 시스템 - 카드 확률/획득/수집 UI~~ (완료)
2. ~~배틀 시스템 (턴제 자동전투, PvP 1:1)~~ (완료)
3. ~~멀티플레이 (방 생성/초대, 실시간 동기화)~~ (완료)
4. ~~프론트엔드 화면 연결 (닉네임/방 생성·참가, 로비, 가챠, 카드 보관함·덱 편성, 배틀 결과)~~ (완료)
5. ~~싱글 플레이 (AI 상대) + 시나리오 캠페인 12전투 x Lv1~5~~ (완료)

## 9. 상태
가챠 + 5:5 라운드제 배틀 엔진(장수/스킬/대상 직접 선택, AI 위임 토글, 일기토) +
방 제목/목록이 있는 로비·웹소켓 멀티플레이 + AI 상대 싱글 플레이 +
시나리오 캠페인(12전투 x 5레벨 = 60스테이지, 진행도 저장, 고정덱/내 덱 선택) +
일러스트·전투 배경(Bedrock 생성) + 프론트엔드까지 전 구간 연결 완료.

남은 것으로 생각해둔 것:
- PvE 협동전 엔진 (방 최대 4명 구조는 이미 있음)
- 강화(재료) 시스템, 링 유료결제(지금은 mock), 클라우드 배포
- 선공 이점이 62%로 큰 편 - 조정할지 결정 필요
- 로그 문구의 한국어 조사 처리 (`관우이(가)`, `황충을(를)`)
- 특정 타이밍의 연결 종료에서 나는 `RuntimeError: WebSocket is not connected` (로그만 지저분)
