# 앞으로 할 일

착수했다가 미뤄둔 작업과, 알고는 있지만 아직 안 고친 것들을 적어둔다.
왜 그렇게 하기로 했는지까지 남겨서 나중에 다시 판단하지 않아도 되게 한다.

---

## 1. 시나리오 진영 쏠림 (미착수, 설계만 끝남)

### 문제

시나리오 12개 전투에서 플레이어가 맡는 진영이 한쪽으로 쏠려 있다.
(2026-09-18 측정, `fixed_deck` 구성원의 소속으로 집계)

| 내 진영 | 수 | 전투 |
|---------|----|------|
| 촉 | 9 | 황건적의 난, 반동탁 연합, 장판파, 화용도, 한중공방전, 정군산, 번성전투, 이릉대전, 오장원 |
| 오 | 2 | 적벽대전, 합비전투 |
| 위 | 1 | 관도대전 |

적 진영은 12번 중 8번이 위나라라, 사실상 "촉 vs 위"를 반복하게 된다.

추가로 **이릉대전과 번성전투는 역사적으로 촉이 진 전투인데 촉 시점으로 되어 있다.**

### 원인

각 전투를 삼국지연의의 주인공 시점으로만 잡았다. 연의가 촉 중심 서술이라
유명 전투의 "주역"을 그대로 따라가면 자동으로 촉에 쏠린다. 219년 세 전투
(한중·정군산·번성)가 연달아 촉 시점인 것도 같은 이유다.

### 결정: 전투마다 진영 선택제

각 전투를 양쪽 어느 진영으로든 플레이할 수 있게 한다. 적벽을 주유로도 조조로도,
오장원을 제갈량으로도 사마의로도 치는 식. 60 -> 120 스테이지가 되고 재플레이
가치가 크게 늘어난다.

**미룬 이유**: 데이터 구조 · DB · API · 화면을 전부 손봐야 해서 30~45분 규모다.
급하지 않아 보류했다.

### 더 싼 대안 (10분 규모)

진영을 고르게 하는 대신, 전투별 주역만 재배치해서 쏠림을 없앤다.
화용도는 조조 탈출전, 이릉은 육손, 오장원은 사마의, 번성은 여몽의 형주 기습으로
바꾸면 대략 촉 4 / 위 4 / 오 2 / 혼합 2가 된다.
`scenario_data.py`의 명단과 고정덱만 고치고 캘리브레이션을 다시 돌리면 끝이라
DB도 화면도 건드리지 않는다. 다만 전투마다 진영이 하나로 고정되는 건 그대로다.

### 구현 계획 (진영 선택제로 갈 경우)

**1) 데이터 구조** — `backend/app/scenario_data.py`

전투 하나가 진영 둘을 갖는 형태로 바꾼다. 진영마다
`pool`(적으로 나올 때 쓰는 명단, 앞 3명은 모든 레벨에 출전) 과
`fixed`(내가 고를 때 받는 고정덱 5명) 를 따로 둔다. 둘은 겹쳐도 된다.

```python
{
  "key": "huarong", "name": "화용도", "year": 208, "scene": "huarong",
  "camps": [
    {"id": "guanyu", "label": "관우군", "faction": "촉",
     "intro": "패주하는 조조 앞을 막아선 것은, 옛 은혜를 아는 관우였다.",
     "goal": "조조를 놓치지 마라",
     "pool":  ["관우", "장비", "조운", "관평", "요화", "유비", "제갈량"],
     "fixed": ["관우", "관평", "요화", "장비", "조운"]},
    {"id": "caocao", "label": "조조군", "faction": "위",
     "intro": "적벽에서 모든 것을 잃었다. 남은 것은 좁은 화용도 진흙길뿐.",
     "goal": "포위를 뚫고 살아 돌아가라",
     "pool":  ["조조", "장료", "허저", "서황", "장합", "조인", "조홍"],
     "fixed": ["조조", "장료", "허저", "서황", "조인"]},
  ],
}
```

함수 시그니처도 진영을 받도록 바꾼다:
`build_enemy_lineup(battle, camp_id, level)` / `build_fixed_lineup(...)` /
`stage_enemy_mods(battle_key, camp_id, level)`.

아래는 12전투 x 2진영 명단 초안. 전부 현재 로스터 125명 안에 있는 이름인지
확인한 것이다 (`fixed`는 각 진영 `pool`에서 고른 5명).

| 전투 | 진영 A (pool / fixed) | 진영 B (pool / fixed) |
|------|----------------------|----------------------|
| 황건적의 난 | **관군** 유비·관우·장비·공손찬·조조·하후돈·조인 / 유비·관우·장비·공손찬·조조 | **황건적** 장각·장보·장량·관정·한수·기령·장훈 / 장각·장보·장량·관정·한수 |
| 반동탁 연합 | **연합군** 원소·조조·유비·관우·장비·손책·공손찬·하후돈 / 원소·조조·유비·관우·장비 | **동탁군** 동탁·여포·화웅·초선·장수·가후·한수 / 동탁·여포·화웅·장수·가후 |
| 관도대전 | **조조군** 조조·하후돈·허저·장료·서황·악진·이전·조인·전위 / 조조·하후돈·허저·장료·서황 | **원소군** 원소·안량·문추·고람·저수·전풍·심배·곽도·봉기 / 원소·안량·문추·저수·심배 |
| 장판파 | **유비군** 조운·장비·유비·제갈량·감부인·미씨·관우 / 조운·장비·유비·제갈량·감부인 | **조조군** 조조·하후돈·조인·조홍·문빙·장합·악진·이전 / 조조·하후돈·조인·장합·문빙 |
| 적벽대전 | **손유 연합** 주유·제갈량·황개·감녕·정보·한당·태사자·유비 / 주유·제갈량·황개·감녕·정보 | **조조군** 조조·장료·서황·악진·이전·문빙·하후돈·조인 / 조조·장료·서황·악진·문빙 |
| 화용도 | **관우군** 관우·장비·조운·관평·요화·유비·제갈량 / 관우·관평·요화·장비·조운 | **조조군(탈출)** 조조·장료·허저·서황·장합·조인·조홍 / 조조·장료·허저·서황·조인 |
| 합비전투 | **오 원정군** 손권·감녕·능통·태사자·여몽·진무·반장·서성 / 손권·감녕·능통·태사자·여몽 | **위 수비군** 장료·이전·악진·조인·조휴·문빙·하후상 / 장료·이전·악진·조인·문빙 |
| 한중공방전 | **유비군** 유비·법정·황충·조운·마초·장비·위연·엄안 / 유비·법정·황충·조운·마초 | **조조군** 조조·하후연·장합·서황·조홍·조진·곽회 / 조조·하후연·장합·서황·조홍 |
| 정군산 | **촉군** 황충·법정·엄안·유비·위연·조운·마초 / 황충·법정·엄안·유비·위연 | **위군** 하후연·장합·조진·곽회·조홍·서황 / 하후연·장합·조진·곽회·조홍 |
| 번성전투 | **관우군** 관우·관평·요화·마량·유비·장비·조운 / 관우·관평·요화·마량·유비 | **위·오 연합** 조인·서황·방덕·여몽·육손·장료·서성 / 조인·서황·방덕·여몽·육손 |
| 이릉대전 | **촉군** 유비·황충·마량·조운·요화·위연·장익 / 유비·황충·마량·조운·요화 | **오군** 육손·한당·주태·감녕·정봉·서성·반장·손권 / 육손·한당·주태·정봉·반장 |
| 오장원 | **촉군** 제갈량·강유·위연·왕평·마대·요화·장익 / 제갈량·강유·위연·왕평·마대 | **위군** 사마의·사마사·사마소·곽회·등애·진태·종회·하후패 / 사마의·사마사·사마소·곽회·등애 |

**2) 캘리브레이션** — `backend/dev_scripts/calibrate_scenario.py`

`STAGE_ENEMY_POWER`가 (전투 x 진영 x 레벨) 120칸이 된다. 스크립트는 루프에
진영만 하나 더 돌리면 되고, 실행 시간은 여전히 2~3분이다
(전투 1판이 약 0.5ms).

**주의**: 진영을 바꾸면 고정덱과 적 덱이 통째로 바뀌므로 기존 60칸 값은
전혀 재활용할 수 없다. 반드시 전체를 다시 뽑아야 한다.

**3) DB** — `scenario_progress`에 `camp` 컬럼 추가

`UNIQUE(player_id, battle_key, camp, level, deck_mode)` 로 바꾼다.

**함정**: `schema.sql`은 `CREATE TABLE IF NOT EXISTS`를 `executescript`로
매 기동마다 돌리는 구조라, 기존 테이블의 모양은 바꾸지 못한다. UNIQUE 제약은
`ALTER TABLE ADD COLUMN`으로도 못 고친다. `scenario_progress`는 시나리오와
함께 방금 만든 테이블이라 버려도 되므로, `db.py`의 `init_db()`에서
executescript **전에** 아래처럼 처리하는 게 간단하다.

```python
cols = {r[1] for r in conn.execute("PRAGMA table_info(scenario_progress)")}
if cols and "camp" not in cols:
    conn.execute("DROP TABLE scenario_progress")   # 이후 schema.sql이 새로 만든다
```

**4) 해금 규칙**

진영별로 따로 간다. 촉으로 Lv1을 깨면 촉 Lv2가 열리고, 위는 여전히 Lv1부터다.
그래야 양쪽이 각각 의미 있는 진행이 된다. `unlocked_level()`에 camp를 넘긴다.

**5) API** — `backend/app/main.py`, `scenario.py`

- `GET /scenario/battles` 응답의 각 전투에 진영 2개와 진영별 진행도를 넣는다
- `GET /scenario/battles/{key}/camps/{camp}/levels/{lv}`
- `POST /rooms/{code}/scenario` 요청에 `camp` 추가
- `Room.scenario`에도 `camp`를 넣어 정산 때 쓴다

**6) 화면** — `frontend/index.html`, `app.js`

`#screen-scenario-stage`의 레벨 버튼 위에 진영 토글을 둔다. 진영을 바꾸면
레벨 해금 상태와 적 진용 미리보기가 같이 바뀐다. 전투 목록 타일의 별 진행도는
두 진영 합산(n/10)으로 보여주거나 진영별로 두 줄로 나눈다.

---

## 2. 그 밖에 알고 있는 것들

- **선공 이점이 62%로 큰 편이다.** 무력이 가장 높은 카드를 가진 쪽이 선공을 가져간다.
  "빠른 덱이 유리"라는 의도로 볼 수도 있어 손대지 않았다. 줄이려면 선공 측 첫
  라운드에 페널티를 주거나, 선공을 무작위로 돌리는 방법이 있다.
  **주의**: 이걸 바꾸면 시나리오 캘리브레이션을 다시 돌려야 한다.
- **로그의 한국어 조사 처리.** `관우이(가)`, `황충을(를)` 처럼 나온다.
  받침 유무로 조사를 고르는 헬퍼를 하나 만들어 `battle.py`의 문구에 적용하면 된다.
- **특정 타이밍의 연결 종료에서 `RuntimeError: WebSocket is not connected`.**
  서버 로그만 지저분해지고 동작에는 영향이 없다.
- **시나리오에 안 나오는 장수 47명** (여성 장수 다수, 후기 인물 등). 가챠와 대전에는
  나오므로 문제는 아니지만, 전투를 더 늘린다면 이들을 쓸 자리가 생긴다.

---

## 바꾸면 반드시 다시 돌려야 하는 것

장수 스탯(`seed_data.py`), 전투 공식(`battle.py`), 시나리오 명단·등급 사다리를
건드리면 난이도가 통째로 틀어진다. 손으로는 못 맞추므로 아래를 돌려서 다시 뽑는다.

```
cd backend
.venv\Scripts\python.exe dev_scripts\calibrate_scenario.py
```

출력된 `STAGE_ENEMY_POWER` 사전을 `app/scenario_data.py`에 붙여넣으면 된다.
